"""
============================================================
  SINGULARITY - Chronicle of Origin -  Prototype 0.7
  core/constants.py  ── ゲーム全体で使う定数まとめ

  [0.7 変更点]
    - タイトルを Prototype 0.7 に更新
    - STATE_DIALOGUE を追加（会話中のゲーム状態）
    - 会話ウィンドウ用カラー定数を追加
      C_DIALOGUE_BG / C_DIALOGUE_BORDER / C_DIALOGUE_NAME / C_DIALOGUE_TEXT
  [0.6 変更点]
    - タイトルを Prototype 0.6 に更新
    - 町用タイル色定数を追加（C_TOWN_FLOOR / C_TOWN_WALL）
    - 出口タイル色定数を追加（C_EXIT_TILE）
    - 既存定数・SLIME_VARIANTS・SLIME_ELEMENT は変更なし
  [0.5 変更点]
    - 属性カラー定数を追加（C_ELEM_FIRE など 7種）
    - SLIME_ELEMENT を新規追加（スライム名 → 属性ID のマップ）
  [0.4 以前の変更点]
    - 属性スライムを基本六属性（火・水・風・土・光・闇）に統一
    - STATE_BATTLE（バトル画面）を追加
    - バトル画面レイアウト用の定数を追加

  ★ ここの数値を変えるだけで、難易度・速度・画面サイズを調整できます！
============================================================
"""

# ── ウィンドウ設定 ─────────────────────────────────────
TITLE    = "SINGULARITY - Chronicle of Origin -  Prototype 0.7"
WINDOW_W = 800   # 画面の横幅（ピクセル）
WINDOW_H = 600   # 画面の縦幅（ピクセル）
FPS      = 60    # 1秒間のフレーム数（大きいほど滑らか）

# ── タイル（マップのマス目） ────────────────────────────
TILE     = 32    # 1マスのサイズ（ピクセル）

# ── プレイヤー設定 ──────────────────────────────────────
PLAYER_SPEED     = 3    # 1フレームあたりの移動量（px）
PLAYER_ATTACK_CD = 25   # 攻撃のクールダウン（フレーム数）

# ── 敵（探索マップ）設定 ─────────────────────────────────
ENEMY_SPEED      = 1    # 敵の移動速度
ENEMY_ATTACK_CD  = 60   # 敵の攻撃クールダウン（フレーム数）
ENEMY_DETECT_R   = 200  # 敵がプレイヤーを感知する距離（px）

# ── UI レイアウト（探索マップ用） ────────────────────────
HUD_H        = 90                 # 画面下のHUD（情報表示エリア）の高さ
GAME_AREA_H  = WINDOW_H - HUD_H  # ゲームが描画される縦の高さ = 510

# ── バトル画面レイアウト ──────────────────────────────────
# 画面を上部（キャラ表示）と下部（コマンド）に分ける
BATTLE_TOP_H    = 360   # キャラクター表示エリアの高さ
BATTLE_CMD_H    = WINDOW_H - BATTLE_TOP_H   # コマンドウィンドウの高さ = 240
BATTLE_CMD_Y    = BATTLE_TOP_H              # コマンドウィンドウのY座標

# バトル内キャラクター表示位置
PLAYER_BATTLE_X = 160   # プレイヤーの基本X座標（左側）
PLAYER_BATTLE_Y = 200   # プレイヤーのY座標
ENEMY_BATTLE_X  = 560   # 敵の基本X座標（右側）
ENEMY_BATTLE_Y  = 180   # 敵のY座標

# ── カラー定義（RGB形式） ──────────────────────────────
C_BLACK      = (  0,   0,   0)
C_DARK_BG    = ( 12,  10,  18)   # 背景：深い紫黒
C_FLOOR      = ( 28,  24,  38)   # 床タイル
C_WALL       = ( 18,  15,  28)   # 壁タイル
C_GRID       = ( 35,  30,  50)   # グリッド線（薄く）
C_WHITE      = (240, 235, 220)   # テキスト・プレイヤー
C_GOLD       = (201, 168,  76)   # 金色（UI装飾・EXP）
C_CRIMSON    = (160,  30,  30)   # 深紅（HP バー・敵）
C_CRIMSON_LT = (220,  60,  60)   # 明るい深紅
C_VIOLET     = ( 80,  40, 140)   # 魔法紫
C_GREEN_DIM  = ( 40, 120,  60)   # 経験値バー
C_GRAY       = ( 80,  75,  90)
C_DARK_GRAY  = ( 40,  38,  50)

# ── 属性カラー定数（element_system.py の ELEMENT_COLOR と同値） ─
#    UI・HUD・スキル名の色分けに使う。
#    element_system.get_element_color() でも取得できるが、
#    constants.py からも直接参照できるよう定数として定義しておく。
C_ELEM_FIRE  = (220,  70,  30)   # 火：赤橙
C_ELEM_WATER = ( 50, 130, 220)   # 水：青
C_ELEM_WIND  = ( 60, 180,  80)   # 風：緑
C_ELEM_EARTH = (140,  95,  50)   # 土：茶
C_ELEM_LIGHT = (230, 220, 140)   # 光：金白
C_ELEM_DARK  = ( 90,  50, 140)   # 闇：紫黒
C_ELEM_NONE  = (160, 155, 145)   # 無：グレー

# バトル画面用カラー
C_BATTLE_BG     = (  8,   6,  16)   # バトル背景（より暗く）
C_WINDOW_BG     = ( 15,  12,  25)   # コマンドウィンドウ背景
C_WINDOW_BORDER = ( 80,  65, 120)   # ウィンドウの枠線
C_CMD_SELECT    = (201, 168,  76)   # 選択中コマンドの色（金色）
C_CMD_NORMAL    = (180, 170, 160)   # 未選択コマンドの色
C_CMD_DISABLED  = ( 60,  55,  70)   # 使用不可コマンドの色（グレー）
C_DAMAGE_NUM    = (255, 220,  60)   # ダメージ数字の色（黄）
C_HEAL_NUM      = ( 80, 220, 120)   # 回復数字の色（緑）

# ── ゲームの状態を表す文字列 ──────────────────────────
STATE_TITLE    = "title"     # タイトル画面
STATE_PLAY     = "play"      # 探索マップ
STATE_BATTLE   = "battle"    # ★ 0.2新追加：ターン制バトル画面
STATE_LEVELUP  = "levelup"   # レベルアップ演出
STATE_GAMEOVER = "gameover"  # ゲームオーバー画面
STATE_DIALOGUE = "dialogue"  # ★ 0.7新追加：NPC会話中
STATE_PROLOGUE = "prologue"  # ★ 0.7 Step5-D: ゲーム開始直後のプロローグ表示

# ── レベルアップに必要な累積経験値テーブル ────────────
#    EXP_TABLE[lv] = そのレベルになるために必要な総EXP
EXP_TABLE = [0, 100, 250, 450, 700, 1000, 1350, 1750, 2200, 2700, 9_999_999]

# ── 属性スライムのバリアント定義（0.2：基本六属性に統一） ──
#    (名前, 最大HP, 攻撃力, 防御力, 倒したときのEXP, 体の色)
#
#    火：赤橙  水：青  風：緑  土：茶  光：金白  闇：紫黒
SLIME_VARIANTS = [
    ("火スライム", 18, 6, 1, 30, (220,  70,  30)),   # 赤橙
    ("水スライム", 22, 3, 3, 30, ( 50, 130, 220)),   # 青
    ("風スライム", 16, 5, 1, 28, ( 60, 180,  80)),   # 緑
    ("土スライム", 24, 4, 4, 32, (140,  95,  50)),   # 茶色
    ("光スライム", 20, 7, 2, 40, (230, 220, 140)),   # 金白
    ("闇スライム", 26, 8, 2, 45, ( 90,  50, 140)),   # 紫黒（C_ELEM_DARK と統一）
]

# ── スライム属性マップ（0.5 新規） ─────────────────────────
#    スライムの名前（SLIME_VARIANTS の name 列）→ 属性ID のマッピング。
#    SLIME_VARIANTS のタプル形式を変えずに属性IDを参照できるようにする。
#
#    使い方（enemy.py Step6 以降で参照）:
#        from core.constants import SLIME_ELEMENT
#        element_id = SLIME_ELEMENT.get(self.name, "none")
#
#    ★ 新しいスライムを SLIME_VARIANTS に追加したとき、
#      ここにも対応するエントリを追加してください。
SLIME_ELEMENT: dict[str, str] = {
    "火スライム": "fire",
    "水スライム": "water",
    "風スライム": "wind",
    "土スライム": "earth",
    "光スライム": "light",
    "闇スライム": "dark",
}

# ── スプライト画像パス（SpriteManager が参照する） ──────
#    画像ファイルは assets/images/ に置いてください
#    ファイルがなくても図形でフォールバックするので動作します
SPRITE_DIR = "assets/images"

# ── ゾーン別タイル色（0.6 新規） ─────────────────────────
#    zone_data.py / world.py からタイル描画色として参照する。
#    ゾーンごとの雰囲気を色で表現する。

# 始まりの町の床・壁色（ダンジョンより明るい石畳のイメージ）
C_TOWN_FLOOR = ( 45,  40,  55)   # やや明るい石畳（C_FLOOR より明るめ）
C_TOWN_WALL  = ( 35,  30,  48)   # 石造りの壁（C_WALL より明るめ）

# 出口タイルの表示色
# 探索マップ上に出口マーカーとして描画するときに使う。
# 目立つようにゴールド系にしつつ、床と区別できる色にする。
C_EXIT_TILE  = (160, 140,  60)   # くすんだゴールド（出口タイル）

# ── 会話ウィンドウ用カラー（0.7 新規） ─────────────────────
#    NPC 会話ウィンドウの描画に使う。
#    バトルウィンドウ（C_WINDOW_BG 系）より暖かみのある色合いにして
#    「人との会話」の雰囲気を出す。

# ウィンドウ背景（少し茶みがかった暗色 → 石造りの建物内のイメージ）
C_DIALOGUE_BG     = ( 18,  14,  22)   # バトルより少し暖かい暗背景

# ウィンドウ枠線（金色系 → 大賢者・イベントの格式を表現）
C_DIALOGUE_BORDER = (160, 130,  55)   # くすんだゴールド枠

# 話者名テキスト（金色 → 名前を目立たせる）
C_DIALOGUE_NAME   = (201, 168,  76)   # C_GOLD と同値（統一感）

# 本文テキスト（やや暖かい白 → C_WHITE と同値）
C_DIALOGUE_TEXT   = (240, 235, 220)   # C_WHITE と同値（統一感）
